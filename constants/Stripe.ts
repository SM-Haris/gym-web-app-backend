const Stripe = Object.freeze({
  MESSAGES: {
    CHECKOUT_FAILURE:
      'Something went wrong stripe checkout session could not be generated',
    PORTAL_SESSION_FAILURE:
      'Something went wrong stripe portal session could not be created',
  },
})

export default Stripe
